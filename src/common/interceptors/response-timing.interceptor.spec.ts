import { ResponseTimingInterceptor } from './response-timing.interceptor';

describe('ResponseTimingInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseTimingInterceptor()).toBeDefined();
  });
});
